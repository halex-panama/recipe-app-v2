import styled from "styled-components";

export const Wrapper = styled.nav`
  margin: 2rem 0;

  div {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  a {
    text-decoration: none;
  }

  svg {
    font-size: 2rem;
  }
`;
